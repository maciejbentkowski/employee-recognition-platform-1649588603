<h1>Rewards index page</h1>
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Price</th>
    </tr>
  </thead>
  <tbody>
    <% @rewards.each do |reward| %>
      <tr>
        <td><%= link_to reward.title, reward_path(reward) %></td>
        <td><%= reward.price %></td>
      </tr>
    <% end %>
  </tbody>
</table>